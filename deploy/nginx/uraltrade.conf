# Place in /etc/nginx/sites-available/uraltrade
# ln -s /etc/nginx/sites-available/uraltrade /etc/nginx/sites-enabled/
# nginx -t && systemctl reload nginx
#
# SSL: certbot --nginx -d uraltrade.example.com

upstream uraltrade_app {
    server 127.0.0.1:3010;
    keepalive 32;
}

server {
    listen 80;
    server_name uraltrade.example.com;

    # Redirect to HTTPS after certbot, or serve HTTP during initial setup:
    # return 301 https://$host$request_uri;

    client_max_body_size 32M;

    location / {
        proxy_pass http://uraltrade_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 60s;
        proxy_connect_timeout 10s;
    }
}

# Uncomment after obtaining SSL certificate:
#
# server {
#     listen 443 ssl http2;
#     server_name uraltrade.example.com;
#
#     ssl_certificate     /etc/letsencrypt/live/uraltrade.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/uraltrade.example.com/privkey.pem;
#
#     client_max_body_size 32M;
#
#     location / {
#         proxy_pass http://uraltrade_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 60s;
#         proxy_connect_timeout 10s;
#     }
# }
